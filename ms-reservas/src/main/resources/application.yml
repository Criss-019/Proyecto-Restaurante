server:
  port: 8084 # Puerto exclusivo para reservas

spring:
  application:
    name: ms-reservas
  datasource:
    url: jdbc:mysql://localhost:3306/reservas_db?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
