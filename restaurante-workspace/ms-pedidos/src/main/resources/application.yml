server:
  port: 8086 # Puerto exclusivo para pedidos

spring:
  application:
    name: ms-pedidos
  datasource:
    url: jdbc:mysql://localhost:3306/pedidos_db?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

