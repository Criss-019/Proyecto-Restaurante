server:
  port: 8087 # Puerto exclusivo para pagos

spring:
  application:
    name: ms-pagos
  datasource:
    url: jdbc:mysql://localhost:3306/pagos_db?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
